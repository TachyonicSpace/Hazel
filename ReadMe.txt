//https://docs.hazelengine.com/

Run the line below to install
	-> git clone https://github.com/TachyonicSpace/Hazel --recursive
goto scripts and run setup.bat
if you are running the vulkan installer, also install the 
	debuggable shader api libs 64 bit, and re-run the setup.bat
